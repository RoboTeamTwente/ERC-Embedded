syntax = "proto3";
// used to wrap a message 

message DiagnosticMessage {
  int32 level = 1;
  int32 timestamp =2;
}
message PBMessageEnvelope {
  oneof payload {
    bytes pb_broadcast_message = 1;
    DiagnosticMessage diagnostic_message = 2;
  }
}

